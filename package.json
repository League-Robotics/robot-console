{
  "name": "robot-console",
  "private": true,
  "version": "0.20260910.10",
  "type": "module",
  "description": "GUI for managing micro:bit robots built on the Elecfreaks Nezha differential-drive board.",
  "bin": {
    "robot-console": "./bin/robot-console.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "test": "vitest run",
    "build": "npm run build -w @robot-console/protocol && npm run build -w @robot-console/host && npm run build -w @robot-console/ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
