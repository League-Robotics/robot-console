/**
 * @robot-console/ui — placeholder root component.
 *
 * Scaffolding only. Later tickets add the Devices and Console tabs
 * (this sprint) and Telemetry/Trace/Calibrate (later sprints), talking
 * to `packages/host`'s server over one WebSocket.
 */
export function App() {
  return <div>robot-console (scaffolding — no UI yet)</div>;
}
