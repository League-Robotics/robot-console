#!/usr/bin/env node
// robot-console CLI entry point.
//
// This ticket (001) only needs the entry point to exist and run without
// crashing. Starting the actual host server (Express + ws) and opening
// the browser is ticket 009's job, once packages/host/src/server.ts
// exists.

console.log("robot-console: entry point placeholder (no server started yet).");
// TODO(ticket 009): import and start packages/host/src/server.ts, then
// open the student's browser to the local URL it prints.
