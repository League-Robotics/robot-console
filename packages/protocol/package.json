{
  "name": "@robot-console/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Pure TypeScript, zero-I/O wire protocol logic: naming, radio address, banner parsing, v6 line codec and session.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
